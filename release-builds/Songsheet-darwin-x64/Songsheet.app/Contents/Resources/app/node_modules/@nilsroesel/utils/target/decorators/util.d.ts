export declare function createName(key: string, functionType: FunctionTypes): string;
export declare enum FunctionTypes {
    GET = "get",
    SET = "set",
    BUILD = "build",
    BUILDER = "Builder",
    TO_STRING = "toString",
}
